# Job Simulators

This repository contains 20 job simulator projects copied from the `job-simulator-workflow` simulator set.

The UN diplomat simulator is intentionally excluded.

Each simulator lives under `simulators/` and is a standalone project.
